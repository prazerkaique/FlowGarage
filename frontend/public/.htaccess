Options -MultiViews
RewriteEngine On

# Handle public catalog routes with JWT tokens (containing dots)
RewriteRule ^public-catalog/(.*)$ index.html [QSA,L]

# Handle all other routes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]